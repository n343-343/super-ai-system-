<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
        This configuration allows cleartext traffic for localhost,
        which is necessary for local development and testing with the Android emulator.
        For production, you should use HTTPS and configure your domain here.
    -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>
    <!-- Production domains should be configured with HTTPS -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
